import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Turbopack is the default in Next.js 16
  // No webpack config needed — Shiki uses dynamic imports natively
};

export default nextConfig;
